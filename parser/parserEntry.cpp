#include "parserEntry.h"
#include "../base/bfChars.h"
#include <sstream>

parser::parser(std::string &bfFile) 
{
        for(auto i : bfFile){
            if(bfFile[i] != brainfuck::core::BF_SYMBOLS::DOT){

            }
        }
}
